scalar Date

type Query{
    notification_admin(notifId: ID!): AdminNotifications
}

type AdminNotifications{
    _id: ID
    authorized: Personnel
    notifications: [AdminNotification]
    createdAt: Date
}

type AdminNotification{
    type: String
    description: String
    date: Date
    notifId: ID
}